# Database (required for PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/database

# JWT secret for auth (required)
JWT_SECRET=your-secret-key-change-in-production

# Optional: For real LLM-based NLP enrichment and conversational AI chat
OPENAI_API_KEY=sk-...

# Optional: For Gemini-based lead enrichment (scraper uses Gemini when set)
GEMINI_API_KEY=...
